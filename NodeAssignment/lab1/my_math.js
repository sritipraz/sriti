const add = (x, y) => x + y;

const subtract = (x, y) => x - y;
const multiply = (x, y) => x * y;
const divide = (x, y) => x / y;
const pi=Math.PI;

module.exports = {
  add: add,
  subtract: subtract,
  multiply: multiply,
  divide: divide,
  pi: pi,
};